
<h1>Welcome <%= current_user.name %>.</h1><br>

<p>Here is a list of all the other users</p>

<ul>
  <% User.all.each do |user| %>
    <% if user.id != current_user.id %>
      <li><%= user.name %></li>
    <% end %>
  <% end %>
</ul>